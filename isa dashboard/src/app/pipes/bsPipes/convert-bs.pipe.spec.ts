import { ConvertBsPipe } from './convert-bs.pipe';

describe('ConvertBsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertBsPipe();
    expect(pipe).toBeTruthy();
  });
});
